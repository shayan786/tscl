.row
	.col-md-8
		.card.meeting-minutes
			.card-header Meeting Minutes
			.card-body
				- if @mms.count > 0
					%table.table.table-dark
						%thead
							%tr
								%th Title
								%th.text-center Actions
						%tbody
							- @mms.each do |m|
								%tr
									%td= m.title
									%td.text-center
										%a{href: "/operations/meeting-minutes/#{m.id}", "data-toggle" => "tooltip", title: "View", "data-placement" => "bottom"}
											%i{class: "fa fa-info-circle"}
				- else
					.div.empty
						%i{class: "fa fa-frown-o"}
						%h4 Nothing to show here.
	.col-md-4
		= render 'shared/points_table'
		= render 'shared/upcoming_matches'